version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    paths:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*